import { Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <div>
            <Typography sx={{marginTop:'30px'}} variant="h6" gutterBottom component="div">
                       Payment
                    </Typography>
            <Typography sx={{marginTop:'30px'}} variant="h6" gutterBottom component="div">
                        Lazy Developer is Working to build Payment System
                    </Typography>
        </div>
    );
};

export default Pay;